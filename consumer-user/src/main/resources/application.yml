server:
  port: 8200
spring:
  application:
    name: consumer-user

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      #地址格式固定
      defaultZone: http://localhost:8761/eureka/


